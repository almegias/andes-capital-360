[build]
  publish = "."
  command = ""

[functions]
  directory = "netlify/functions"

# Redirect all routes to index.html (SPA behavior)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200